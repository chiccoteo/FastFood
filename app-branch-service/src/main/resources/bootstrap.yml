management:
  endpoints:
    web:
      exposure:
        include: refresh, env
spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888
      name: application